import React from 'react';
import Button from '../elements/proshop/Button';

const Proshop = () => {
    return (
        <div className="container">
            <Button />
        </div>
    )
}

export default Proshop;
